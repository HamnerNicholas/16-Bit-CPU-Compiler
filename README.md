# 16-Bit-CPU-Compiler
A custom compiler, assembler, and 16-bit CPU architecture built from scratch in Logisim. Features a C-inspired language with variables, arrays, arithmetic expressions, loops, conditionals, functions with parameters and return values, assembly passthrough, and a custom assembler targeting a custom instruction set architecture.
